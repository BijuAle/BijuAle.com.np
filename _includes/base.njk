<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <style>
      *,
      *::before,
      *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      @font-face {
        font-family: "Linux Biolinium O";
        src: url("/fonts/LinBiolinum_R.otf") format("opentype");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
      }
      @font-face {
        font-family: "CMU Serif Roman";
        src: url("/fonts/cmunrm.ttf") format("truetype");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
      }

      @font-face {
        font-family: "Garamond Regular";
        src: url("/fonts/GaramondPremrPro.otf") format("opentype");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
      }

      @font-face {
        font-family: "Garamond Regular";
        src: url("/fonts/GaramondPremrPro-It.otf") format("opentype");
        font-weight: normal;
        font-style: italic;
        font-display: swap;
      }

      @font-face {
        font-family: "AireRomanPro";
        src: url("/fonts/AireRomanPro.woff2") format("woff2");
        font-weight: normal;
        font-style: normal;
        font-display: swap;
      }

      @font-face {
        font-family: "AireRomanPro";
        src: url("/fonts/AireBoldPro.woff2") format("woff2");
        font-weight: bold;
        font-style: normal;
        font-display: swap;
      }

      body {
        font-family: "Garamond Regular";
        color: rgb(88 72 34);
        background-color: rgb(194 173 150);
        margin: 0 auto;
        max-width: 800px;
        padding: 0 1.5rem;
      }

      header {
        text-align: center;
        position: relative;
      }

      header h1 {
        font-size: clamp(2rem, 5vw, 3rem);
      }

      header span {
        font-family: "CMU Serif Roman";
        display: block;
        line-height: 90%;
        font-size: clamp(0.75rem, 2vw, 0.95rem);
      }

      header nav {
        font-family: "Linux Biolinium O";
        font-size: clamp(0.9rem, 2.5vw, 1.2rem);
        text-transform: uppercase;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 0.2rem;
        margin: 0.5rem 0;
      }

      header nav a {
        border: 1px solid grey;
        padding: 0.2rem;
        white-space: nowrap;
      }

      main {
        text-align: justify;
      }

      /* headings */
      h1,
      h2,
      h3,
      h4 {
        font-family: "AireRomanPro", Georgia, serif;
        line-height: 1.2;
        margin-top: 1.5rem;
        margin-bottom: 0.75rem;
      }
      h1 {
        font-size: clamp(1.5rem, 4vw, 2rem);
      }
      h2 {
        font-size: clamp(1.3rem, 3.5vw, 1.6rem);
      }
      h3 {
        font-size: clamp(1.1rem, 3vw, 1.25rem);
      }
      h4 {
        font-size: 1rem;
      }

      /* paragraphs */
      p {
        font-family: "Garamond Regular";
        font-size: clamp(1rem, 2.5vw, 1.2rem);
        color: rgb(38, 55, 55);
        margin-bottom: 1.25rem;
      }

      a {
        color: rgb(35 63 100);
        text-decoration: none;
        transition: background-color 0.3s ease;
        border-radius: 5px;
      }
      a:hover {
        background-color: rgb(164, 159, 110);
      }

      /* lists */
      ul,
      ol {
        padding-left: 1.5rem;
        margin-bottom: 1.25rem;
      }
      li {
        margin-bottom: 0.35rem;
        font-family: "Garamond Regular";
        font-size: clamp(1rem, 2.5vw, 1.2rem);
        color: rgb(38, 55, 55);
      }
      li > ul,
      li > ol {
        margin-top: 0.35rem;
        margin-bottom: 0;
      }

      /* blockquote */
      blockquote {
        border-left: 3px solid #ccc;
        padding-left: 1.25rem;
        margin: 1.5rem 0;
        color: #555;
        font-style: italic;
      }

      /* code */
      code {
        font-family: "Courier New", monospace;
        font-size: 0.875em;
        padding: 0.15em 0.35em;
        border-radius: 3px;
        word-break: break-word;
      }
      pre {
        padding: 1.25rem;
        border-radius: 4px;
        overflow-x: auto;
        margin-bottom: 1.25rem;
      }
      pre code {
        background: none;
        padding: 0;
        font-size: 0.875rem;
      }

      /* images */
      img {
        max-width: 100%;
        height: auto;
        display: block;
        margin: 1.5rem 0;
      }

      .web-heading {
        text-align: center;
        position: relative;
        margin:0;
        padding:0;
      }

      .web-heading::after {
        content: "◆ ◆ ◆";
        display: block;
        font-size: 1rem;
        color: #2e7d32;
      }
      header > span:first-of-type {
        font-family: "Garamond Regular";
        font-size: 1rem;
        margin-top: 1rem;
        opacity: 80%;
      }

      /* Mobile adjustments */
      @media (max-width: 600px) {
        body {
          padding: 0 1rem;
        }

        header span {
          font-family: "Garamond Regular";
          font-size: 0.8rem;
        }

        header nav a {
          padding: 0.3rem 0.5rem;
          font-size: 0.9rem;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <span
        >You shall know the truth, <br />& the truth shall set you free.
        <span style="font-style: italic">—John 8:32</span></span
      >
      <a href="/"><h1 class="web-heading">Biju Ale</h1></a>

      <nav>
        <a href="/">Essays</a>
        <a href="/pages/photo-memories">Photo Memories</a>
        <a href="/pages/about">About</a>
      </nav>
      <hr style="color: gray; opacity: 30%" />
    </header>
    <main>
      <h1 style="text-align: center; border-radius: 10px">
        {{ title }}
      </h1>
      {{ content | safe }}
    </main>
  </body>
</html>
