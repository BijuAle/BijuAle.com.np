---
layout: layouts/base.njk
title: Photo Tags
---

<section class="photo-tags">
  <div class="tag-grid">
    {% for tag in photos.tags %} {% if photos.byTag[tag] and
    photos.byTag[tag][0] %}
    <figure class="tag-card">
      <a href="/photo-tag/{{ tag | url }}">
        <figcaption>{{ tag | titleCase }}</figcaption>
        <img
          src="{{
            photos.byTag[tag][0].secure_url
          }}?w=300&h=300&c=fill&f=auto&q=auto"
          alt="Photo representing {{ tag }}"
          width="100"
          height="100"
        />
      </a>
    </figure>
    {% endif %} {% endfor %}
  </div>
</section>

<style>
  .tag-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 4fr));
    gap: 0.5rem;
  }

  .tag-card {
    margin: 0;
    border-radius: 10px;
    overflow: hidden;
    text-align: center;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  .tag-card a {
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .tag-card img {
    width: 100%;
    height: auto;
    border: none;
    transition: border 0.1s;
    border: 1px solid black;
  }

  .tag-card img {
    cursor: pointer;
    -webkit-filter: grayscale(100%);
    filter: grayscale(100%);
    -webkit-transition: 0.2s ease-in-out;
    transition: 0.2s ease-in-out;
    flex-shrink: 0;
  }

  .tag-card img:hover,
  .tag-card img.active-thumb {
    -webkit-filter: grayscale(0);
    filter: grayscale(0);
  }

  .tag-card figcaption {
    padding: 0.3rem 0.75rem;
    color: bkack;
    opacity: 90%;
    font-size: 1.2rem;
    font-weight: bold;
    font-family: "Garamond Italic";
  }
</style>
