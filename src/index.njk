---
layout: layouts/base.njk
---
<h1>Essays</h1>
<ul>
  {% for post in collections.post |reverse %}

    <li>
        &#10032;<a href={{post.url}}>
            {{ post.data.title }}</a>
          <span class="date">{{post.date|formatted}}</span>
        </li>
        {% endfor %}
</ul>
